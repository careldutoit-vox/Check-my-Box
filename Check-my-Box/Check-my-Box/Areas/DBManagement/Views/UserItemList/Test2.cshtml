@model Check_my_Box.Areas.DBManagement.Models.UserItemList

@{
    ViewBag.Title = "Test2";
}

<h2>My CheckList</h2>
    @using (Html.BeginForm()) {
        @Html.ValidationSummary(true)
         @Html.HiddenFor(model => model.UserItemListId)
          @Html.HiddenFor(model => model.UserId)
        <fieldset>
            <legend>Wedding CheckList</legend>
            
            <div class="editor-label">
               Owner
            </div>
            <div class="editor-field">
                @Html.LabelFor(model => model.UserOwner.Name)
            </div>
             

            <div class="editor-field">
                @Html.EditorFor(model => model.UserItems)
            </div>
            <p>
                <input type="submit" value="Save" />
            </p>
        </fieldset>

    }

    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>

